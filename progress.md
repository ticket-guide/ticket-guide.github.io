# Progress

## 2026-03-03
- **작업 시작**: 티켓가이드 웹사이트(상품권 예약판매 안내 가이드) 프로젝트 초기화 및 기획.
- `AGENT.md` 및 `README.md` 요구사항 분석 완료.
- `todolist.md` 초기화.
- NotebookLM을 통해 바이브코딩용 코드 아키텍처 참조 준비.
- 구현 계획서(`implementation_plan.md`) 작성 및 승인 완료.
- Next.js 15 App Router 환경 세팅 및 정적 사이트 Export 배포(`gh-pages`) 준비 완비.
- AIDE(기능 기반 아키텍처) 폴더 구조에 맞춰 상담/문의 모달 (`ticket-consulting`) 캡슐화 개발 완료.
- **[추가 콘텐츠]** 참조 사이트를 기반으로 예약판매/주의사항/방침 등의 텍스트를 최신 UI(Glassmorphism, Lucide Icon 등)로 구성하여 `guide-content` 도메인 캡슐화 및 `page.tsx` 통합 완료.
- 안내 섹션 중 '거래 과정 예시' 부분을 실제 채팅을 주고받는 형태의 친화적인 UI(말풍선, 상대방 프로필 추가 등 UX 고도화)로 수정 완료.
- 안내 섹션 중 '거래 과정 예시' 부분을 실제 채팅을 주고받는 형태의 친화적인 UI(말풍선, 상대방 프로필 추가 등 UX 고도화)로 수정 완료.
- Github 연동 시 불필요하게 올라갈 수 있는 파일(`.next/`, `node_modules/` 등)에 대한 `.gitignore` 설정을 최신 Next.js/Node 기반 템플릿으로 추가 적용 완료.
- Github Pages 연동 시 사용자 계정 서브 디렉토리 문제로 인한 CSS/에셋 깨짐 에러 대비를 위해 `next.config.ts`에 필요 시 주석을 해제할 수 있도록 `basePath`, `assetPrefix` 안내 코드 삽입 및 배포 재안내.
- **[오류 해결 및 개선]** 브라우저 탭에 표시되는 아이콘(Favicon) 404 에러를 해결하기 위해 상품권 모양의 커스텀 SVG 파비콘(`public/icon.svg`)을 제작하여 메타데이터에 연동 완료.
- 메인 타이틀 및 브라우저 탭 제목을 '상품권 안내 가이드'에서 **'상품권 판매 안내 가이드'**로 변경 완료.
- **[UI 전면 개편]** 기존 단순 상담/판매 버튼들을 제거하고, 여러 업체를 카테고리별로 모아볼 수 있는 Grid(PC) & List(Mobile) 기반의 업체 목록 UI 컴포넌트(`CompanyListSection`)로 신규 개발 및 대체.
- **[UI 전면 개편]** 기존 단순 상담/판매 버튼들을 제거하고, 여러 업체를 카테고리별로 모아볼 수 있는 Grid(PC) & List(Mobile) 별칭의 업체 목록 UI 컴포넌트(`CompanyListSection`)로 신규 개발 및 대체.
  - 마우스 동작 시 다이내믹한 렌더링을 위해 Float(호버 시 상단으로 부유), 맥박처럼 빛나는 Pulse-glow, 빛이 사선으로 스쳐지나가는(Shine) 텍스처 애니메이션을 `tailwind.config.ts`와 `globals.css`에 구축하여 트렌디하게 연출.
  - 업체명 길이가 다르더라도 하단 '실시간 상담 연결' 버튼과 해시태그 위치가 균일하게 고정되도록 `flex-col`, `h-full`, `mt-auto` CSS 설정을 적용하여 카드 레이아웃 정렬 최적화 완료 (별점 항목 제거).
- **[배경 디자인 개선]** 기존의 단조로운 단색 배경에서 벗어나, 15초마다 자연스럽게 색상이 흐르는(Linear Gradient Animation) 트렌디한 백그라운드 효과를 `globals.css`의 `body` 영역에 적용하여 서비스 퀄리티를 한 단계 업그레이드.
- **[UI 최소화]** 불필요해진 '취급 상품권 종류' 카테고리 필터 버튼 묶음과 각 카드 내부의 카테고리 태그들을 데이터 및 코드 레벨에서 전부 깔끔하게 삭제하여 직관성을 높임.
- **[구조 및 레이아웃 개선]** `page.tsx`에 분리되어 있던 메인 타이틀 및 소개 문구를 `guide-content/components.tsx` 내부로 병합하여 구조적 안정성을 높였으며, PC에서는 1줄, 모바일에서는 2줄로 자연스럽게 표시되도록 반응형 줄바꿈(`sm:hidden` 활용) 처리를 완료하여 텍스트 짤림 현상을 해결함.
- **[UI 컨테이너/색상 개선]** 메인 타이틀 파트와 '예약판매란?' 첫 번째 설명 섹션을 시각적으로 이어지는 하나의 큰 부모 컨테이너 모양(rounded-3xl, shadow)으로 묶고, 상단과 하단의 내부 블록 배경색을 분리하여 통일감 있으면서도 입체적인 화면을 제공함.
- **[업체 리스트 스타일 고도화]** `logic.ts` 및 `types.ts`에 `themeKey` 속성을 추가하고, 각 업체 렌더링 시 고유의 배경 퍼스널 컬러(blue, purple, emerald, rose 등)가 입혀지도록 디자인 차별화 수행. (과도한 둥실거림 등 호버 애니메이션 삭제)
- **[퀵 내비게이션 추가]** 메인 화면 상단에서 하단의 '업체 리스트' 구역으로 한 번에 스크롤 이동이 가능한 바로가기(Anchor) 앵커 버튼 삽입 완료.
- **[반응형 및 모바일/PC 호환성 개선]** 컨테이너 크기를 특정 픽셀로 강제하지 않고 Tailwind의 반응형(max-w-5xl, grid-cols-1~2)을 사용하여, 모바일에 최적화하면서 PC 등의 넓은 화면에서도 자연스럽고 호환성 높게 보이도록 레이아웃 재수정 완료.
- **[업체 리스트 스타일 고도화]** 각 제휴 업체 카드의 지저분한 테투리 선(border)을 전부 제거하고, 가독성과 시인성을 위해 배경톤을 한층 더 진하고 선명하게 올리는(ex. bg-blue-200, shadow-lg) 방식으로 업체 카드 컴포넌트 디자인 전면 수정 완료.
- **[안내 목차 빠른 이동 메뉴 고도화]** `page.tsx` 내비게이션의 요구사항을 반영해, 안내 섹션(예약판매란, 주의사항, 법적처벌, 업체바로가기)으로 바로 점프(Scroll-margin 적용) 할 수 있는 멀티 앵커 버튼 바 형태로 확장 구축 성공.
- **[업체 리스트 아코디언 기능 추가]** 리스트 상단에 토글(열기/닫기) 헤더 버튼을 배치하여, 고객이 긴 업체 리스트를 자유롭게 접거나 펼쳐서 볼 수 있는 UI/UX 인터랙션을 새롭게 구현.
- **[다크 모드 디자인 전면 적용]** 눈이 편안한 환경 제공을 위해 `globals.css`의 전역 그라데이션 베이스를 블루 톤에서 다크 그레이 계열로 교체하고, 투명도를 적용하던 기존 흰색 계열 컨테이너들을 전부 `bg-zinc-800` ~ `bg-zinc-900` 급의 어두운 다크 톤 디자인으로 리뉴얼. 메인 타이틀 텍스트 또한 `text-white` 기반으로 시인성을 극대화.
- **[모바일 타이틀 줄바꿈 방지]** 모바일 화면에서 '상품권 예약판매 거래 가이드', '미발송 시 형사/민사상 법적 처벌', '신뢰와 약속이 가장 중요합니다' 등 중요한 메인 텍스트가 여러 줄로 깨지는 현상을 제어하고자 `whitespace-nowrap`, `tracking-tighter` 및 모바일 전용 폰트 사이즈 조절 클래스들을 적용하여 깔끔한 한 줄 렌더링이 되도록 최적화.
- **[아코디언 및 카드 여백(비율) 최적화]** 업체 리스트가 접혀있을 때 하단 섹션과의 불필요한 공백을 제거하고, 개별 업체의 카드 내부 패딩(Padding)과 마진(Margin) 요소, 하단 버튼 높이(`py-4` -> `py-3`)를 줄여 세로 높이를 축소하고 가로 영역을 더 넓게 활용하도록 디자인 비율 수정 완료.
- **[최대 4열(Column) 그리드 / 가로 폭 대폭 확장]** 화면 폭 제한을 기존 `max-w-5xl/7xl`에서 나아가 초강력 와이드 스크린 대응급인 `max-w-[100rem]`(약 1600px) 급으로 다시 한 번 넓혀 좌우 공간 활용도를 극대화하고, 넓어진 공간을 활용해 업체 카드가 컴팩트하고 촘촘히 4열(`grid-cols-4`)로 더욱 넓게 노출되도록 조정.
- **[업체 카드 '특징' 요소 제거]** 카드 하단에 노출되던 '업체 특징' 기능 칩 요소 영역을 완전히 삭제하여 카드의 세로를 최소화하고 내용에만 집중하도록 디자인 심플화.
- **[업체 리스트 샘플 데이터 확충]** 4열 그리드 레이아웃(총 4칸)이 화면에 어떻게 노출되는지 보다 명확히 테스트하고 채울 수 있도록, 기존 4개의 업체에 더해 4개의 더미 업체(`company-e` ~ `company-h`)를 로직 데이터에 추가하여 총 8개 업체 카드가 연속으로 렌더링되게 반영함.
- **[업체명 좌측 왕관/엠블럼 아이콘 적용]** `public` 폴더 안에 존재하는 투명 이미지들(`king-ticket.png`, `rolls-royce.png`, `H.png`, `all-ticket.png`, `h-ticket.png`, `j-ticket.png` 등)을 활용하여, 각 업체 카드의 상호명 좌측에 업체 등급이나 특징을 나타내는 엠블럼형 아이콘으로 렌더링되도록 `iconUrl` 매핑 기능을 개발 및 적용.
- **[카드 사이즈 최적화를 통한 항상 3열 고정 뷰]** 가로폭이 매우 넓어진 상태에서도 카드가 시원시원하고 꽉 찬 느낌을 줄 수 있도록, 사용자의 피드백을 반영해 동적/4열 그리드 로직을 폐기하고 오직 최대 3열(`grid-cols-3`)로만 출력되도록 구조를 대칭 및 고정형으로 단순화. 이를 통해 개별 카드의 넓이가 넓어져 정보 전달력이 비약적으로 상승함.
- **[업체 식별자(ID) 정수형 도입 및 오름차순 자동 정렬]** 데이터 식별자인 업체 `id`를 기존 `string` 타입에서 `number(int)` 타입으로 마이그레이션 처리하였고, 화면에 업체를 나열할 때 `id` 값을 기준으로 오름차순(1, 2, 3...)으로 정렬되어 보여지도록 `logic.ts` 렌더링 함수에 `sort()` 로직을 추가 반영함.
- **[10초 주기 자동 리스트 순환 애니메이션 도입]** 고정된 업체 목록으로 인한 지루함을 없애고 모든 업체가 골고루 최상단에 노출될 수 있도록, `setInterval`과 상태(State)를 결합하여 10초마다 1번째 카드가 맨 뒤로 밀려나며 전체 리스트가 당겨지는 로테이션 로직을 구축함.
- **[외부 라인(LINE) 메신저 다이렉트 딥링크 연결 기능]** 특정 업체(`일등 티켓`)의 경우 웹페이지 자체 QR 모달을 거치지 않고 사용자 기기의 LINE 앱(라인 ID: `ticket2387`)으로 즉시 연결 요청을 받았기에, 데이터 모델(`types.ts`)에 `contactLink`라는 선택적 속성을 신설하고 버튼 클릭 시 링크 유무에 따라 모달 팝업과 새 창 이동(window.open)을 동적으로 분기 처리하는 유연한 아키텍처를 적용함.
- **[모바일 전용 UI 비활성화 및 데스크탑(PC) 뷰 강제 고정]** 사용자의 요청에 따라 모바일 기기에서도 항상 넓은 PC 버전 레이아웃이 동일하게 노출되도록, Next.js 전역 설정(`layout.tsx`)의 Viewport 메타태그에서 `width: 1280`으로 논리적 해상도를 강제하고 초기 확대 비율(`initialScale`) 옵션을 제거하여 모든 스마트폰 브라우저가 화면을 줌-아웃(Zoom-out)해 PC 화면을 렌더링하도록 뷰포트 아키텍처를 전면 조정함.
